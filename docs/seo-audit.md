# SEO audit — 2026-09-19

## Fixed in this change

- Removed the root canonical that caused `/learn`, `/find-help`, and other routes without page metadata to canonicalize to the homepage.
- Added self-referencing canonicals to `/learn`, `/find-help`, and every article.
- Added Article structured data and article Open Graph metadata.
- Set the document language from `/fr` and `/es` route prefixes at request time.
- Replaced changing build-time sitemap timestamps with a fixed content-review date; generated entity URLs omit dates until source-level update dates exist.
- Removed the invalid placeholder seller record from `ads.txt`.

## Next content work

Prioritize original, cited pages answering actual user questions: how to get identification, replacing documents, shelter intake, tenant protections by jurisdiction, coordinated entry, permanent supportive housing, Housing First evidence, unsheltered-count methodology, and how resource records are verified. Each page needs a named reviewer, reviewed date, primary sources, a unique title and description, 3–6 contextual internal links, and an update cadence.

Do not mass-publish thin location or synonym pages. Improve the resource dataset and local usefulness before expanding indexed city pages.
