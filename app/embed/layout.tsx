// Embed-only layout: no header, footer, consent banner, or ads.
// Designed for iframes on partner sites.
export default function EmbedLayout({ children }: { children: React.ReactNode }) {
  return children;
}
